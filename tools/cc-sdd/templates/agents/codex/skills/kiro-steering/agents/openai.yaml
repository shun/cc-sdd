interface:
  display_name: "Kiro steering"
  short_description: "Create or sync project steering."
  default_prompt: "Use $kiro-steering to create or refresh project steering."

policy:
  allow_implicit_invocation: true
